// Package scanner implements the core matching engine for supply chain
// detection. It checks parsed lockfile entries against a rule index and
// optionally scans the host filesystem for indicators of compromise.
package scanner
