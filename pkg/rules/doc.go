// Package rules handles loading, validation, and indexing of supply chain
// scanner rule files. Rules are stored as JSON following the canonical schema
// defined in pkg/rules/schema.json.
package rules
